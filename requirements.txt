# SVCS Layer 5 True AI Dependencies
google-generativeai>=0.3.0
rich>=12.0.0

# Optional: For enhanced error handling and retry logic
tenacity>=8.0.0

# Multi-language AST parsers
phply>=1.2.6  # PHP AST parser (legacy PHP 5.x/7.x syntax only)
esprima>=4.0.1  # JavaScript AST parser (ES6+ compatible)

# Modern PHP parsers for PHP 7.4+ and 8.x support
tree-sitter>=0.20.0  # Modern parser framework
tree-sitter-php>=0.20.0  # PHP grammar for tree-sitter (supports PHP 8.x)

# Development dependencies (optional)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0
