// Before: Simple control flow
function processValue(x) {
    if (x > 0) {
        return x * 2;
    }
    return 0;
}

function loopData(items) {
    for (let i = 0; i < items.length; i++) {
        console.log(items[i]);
    }
}
