// Before: Simple array access
let items = [1, 2, 3, 4, 5];
let first = items[0];
let last = items[items.length - 1];

function getItem(arr, index) {
    return arr[index];
}

let data = { users: ['Alice', 'Bob'] };
let user = data.users[0];
